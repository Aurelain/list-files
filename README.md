# list-files

## Usage
1. Visit [list-files](https://aurelain.github.io/list-files/) (or open `index.html` locally)
2. Feed directories (or files) using any of the following methods:


## Result
The files will be displayed in a flat column (regardless of depth).
If the file is a picture, it will be previewed. If two or more directories contain the same file name (!), the files
will be merged into a single entry and a brief comparison will be generated.

